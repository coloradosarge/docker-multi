sudo: required
service:
   - docker

before_install:
   - docker build -t linuxace/react-test -f ./client/Dockerfile.dev ./client

script:
   - docker run -e CI=true linuxace/react-test npm test

after_success:
  - docker build -t linuxace/multi-client ./client
  - docker build -t linuxace/multi-nginx ./nginx
  - docker build -t linuxace/multi-server ./server
  - docker build -t linuxace/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push linuxace/multi-client
  - docker push linuxace/multi-nginx
  - docker push linuxace/multi-server
  - docker push linuxace/multi-worker